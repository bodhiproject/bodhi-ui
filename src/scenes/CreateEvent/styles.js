const styles = (theme) => ({
  outcomeWrapper: {
    position: 'relative',
  },
  removeOutcome: {
    position: 'absolute',
    top: theme.padding.unit.px,
    right: 0,
    cursor: 'pointer',
  },
});

export default styles;
