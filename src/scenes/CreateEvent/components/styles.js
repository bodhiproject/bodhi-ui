const styles = (theme) => ({
  pickerTab: {
    background: theme.palette.background.default,
  },
  pickerPaper: {
    width: 400,
    height: 330,
  },
  pickerCalendar: {
    background: theme.palette.background.default,
    overflow: 'hidden',
    paddingLeft: 0,
    paddingRight: 0,
  },
});

export default styles;
