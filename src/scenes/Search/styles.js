const styles = (theme) => ({
  searchTabWrapper: {
    position: 'sticky',
    background: theme.palette.background.paper,
  },
  searchTabButton: {
    marginTop: '0px',
    marginBottom: '0px',
  },
  searchTabContainer: {
    marginTop: 20,
  },
});

export default styles;
