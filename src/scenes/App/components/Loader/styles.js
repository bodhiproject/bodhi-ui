const styles = (theme) => ({
  loaderWrapper: {
    background: theme.palette.background.default,
    position: 'fixed',
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
  },
});

export default styles;
