const styles = (theme) => ({
  pickerTab: {
    margin: 0,
    marginTop: 0,
    paddingTop: 10,
    padding: 10,
  },
  subTableContainer: {
  },
  mainTableContainer: {
    marginTop: theme.padding.md.px,
  },
});

export default styles;
