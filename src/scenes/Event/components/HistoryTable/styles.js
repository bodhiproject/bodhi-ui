const styles = (theme) => ({
  pickerTab: {
    margin: 0,
    marginTop: 0,
    paddingTop: 10,
    padding: 10,
    flexGrow: 1,
  },
  subTableContainer: {
    // height: 400,
    // overflowY: 'auto',
  },
  mainTableContainer: {
    marginTop: theme.padding.md.px,
  },
});

export default styles;
