const styles = (theme) => ({
  pickerTab: {
    margin: 0,
    marginTop: 0,
    paddingTop: 10,
    padding: 10,
  },
  subTableContainer: {
  },
  mainTableContainer: {
    marginTop: theme.padding.space5X.px,
  },
});

export default styles;
