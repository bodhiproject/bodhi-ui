const styles = (theme) => ({
  detailTxWrapper: {
    marginTop: theme.padding.md.px,
    overflowY: 'auto',
  },
  detailTxTitle: {
    marginBottom: theme.padding.xs.px,
  },
  arrowSize: {
    fontSize: '8px',
  },
});

export default styles;
