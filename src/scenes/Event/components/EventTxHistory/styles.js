const styles = (theme) => ({
  detailTxWrapper: {
    marginTop: theme.padding.md.px,
  },
  detailTxTitle: {
    marginBottom: theme.padding.xs.px,
  },
  hide: {
    display: 'none',
  },
  show: {
    display: 'table-row',
  },
  arrowSize: {
    fontSize: '8px',
  },
});

export default styles;
