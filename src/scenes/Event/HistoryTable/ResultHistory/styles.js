const styles = (theme) => ({
  detailTxWrapper: {
    marginTop: theme.padding.space5X.px,
    overflowY: 'auto',
  },
  detailTxTitle: {
    marginBottom: theme.padding.space2X.px,
  },
  arrowSize: {
    fontSize: '8px',
  },
});

export default styles;
