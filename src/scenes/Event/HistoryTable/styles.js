const styles = (theme) => ({
  pickerTab: {
    margin: 0,
    marginTop: 0,
    paddingTop: 10,
    padding: 10,
    maxWidth: '100%',
  },
  subTableContainer: {
  },
  mainTableContainer: {
    marginTop: theme.padding.space5X.px,
  },
  tabs: {
    marginBottom: theme.padding.space3X.px,
  },
});

export default styles;
