const styles = (theme) => ({
  snackbar: {
    width: 200,
    marginBottom: theme.padding.unit.px,
  },
  totalCountText: {
    fontWeight: theme.typography.fontWeightBold,
  },
});

export default styles;
