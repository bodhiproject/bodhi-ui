const styles = (theme) => ({
  txidLabel: {
    width: '150px',
  },
  txidRow: {
    position: 'relative',
  },
  txidWrapper: {
    position: 'absolute',
    left: '24px',
    top: '4px',
    bottom: '4px',
    right: '24px',
  },
  txIdText: {
    color: theme.palette.primary.main,
    cursor: 'pointer',
  },
});

export default styles;
