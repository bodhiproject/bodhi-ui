

const styles = (theme) => ({
  invalid: {
    '& > div': {
      'background-color': theme.palette.error.dark,
    },
  },
  colorSecondary: {
    backgroundColor: '#fff !important',
  },
});

export default styles;
