const styles = (theme) => ({
  button: {
    padding: `${theme.padding.spaceX.px} ${theme.padding.space3X.px} ${theme.padding.spaceX.px} ${theme.padding.space2X.px}`,
  },
});

export default styles;
