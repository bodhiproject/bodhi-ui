const styles = (theme) => ({
  button: {
    marginBottom: theme.padding.xs.px,
    padding: `${theme.padding.unit.px} ${theme.padding.sm.px} ${theme.padding.unit.px} ${theme.padding.xs.px}`,
  },
});

export default styles;
